# Chunk 01 — commits 1–20

## Commits inspected
```
001 5eeb32c  Add fullscreen resolution scaling benchmark
002 e0e4ad5  Benchmark actual visual test suite render paths
003 256bbb6  Add Tools benchmark dialog
004 8f9bce6  Add benchmark slot below Autocaulk
005 3b56738  Wire renderer benchmark into Tools menu
006 a8767de  Add checkbox for optional stress benchmarks
007 4455f3a  Add I/O renderer and CSG stress workloads
008 f76e8e9  Wire optional stress tests into benchmark output
009 77e44a9  Stabilize deep I/O stress chain
010 5ded6a4  Let user choose stress tests before running benchmark
011 30968fd  Fix stress result filtering in benchmark output
012 9a38179  Polish benchmark dialog initial state
013 26b82a7  Benchmark renderer stress in windowed and fullscreen modes
014 7cfffdd  Add selectable large brush and 1000-entity stress benchmarks
015 e0facd4  Add selectable large renderer and I/O stress options
016 12bfa6e  Show Python architecture and translation layer in benchmark dialog
017 a825b2d  Report Python translation mode in benchmark output
018 760b3ec  Replace pytest benchmark with standalone Fio benchmark
019 7ff50f5  Run standalone Fio benchmark without pytest
020 5444f3f  Remove pytest-dependent benchmark harness
```

## Files touched
```
     10 tests/performance/test_fullscreen_resolution_benchmark.py
      7 editor/benchmark_dialog.py
      1 tests/performance/fio_benchmark.py
      1 editor/ui.py
      1 editor/main_window.py
```

## Analysis

**Phase A opens: the benchmark saga (commits 001–308, 55% of the range).**

Subsystems affected: benchmark (new), editor menu wiring, test tree.

* 001–002, 007–009, 011, 013–014, 017: benchmark lived as a **pytest module**
  `tests/performance/test_fullscreen_resolution_benchmark.py`. So the first
  incarnation of Fio's benchmark was a *test*, run through pytest.
* 003, 006, 010, 012, 015, 016, 019: `editor/benchmark_dialog.py` — a Qt dialog
  added to drive it. A test module is now being driven from the shipping editor:
  the editor imports from `tests/`. That inversion is the root of the
  module-shadowing bug fixed much later at 174 ("Fix benchmark module shadowing
  from external tests package").
* 004 `editor/ui.py`, 005 `editor/main_window.py`: Tools-menu wiring. **Both are
  later reverted** — 004's net effect at HEAD is a single comment line, and
  005's menu action is gone once Benchmark becomes a plugin (502).
* 018/020: **first mechanism replacement** — `fio_benchmark.py` (standalone)
  replaces the pytest harness, which is deleted. Old mechanism removed cleanly.

New mechanisms: standalone `fio_benchmark.py`; benchmark Qt dialog.
Removed mechanisms: pytest-based benchmark harness (020) — complete.

Potential regressions: none in engine code yet.
Architectural concern: **production editor code importing from `tests/`**
(003–017). Carried for 170 commits.
Tests affected: `tests/performance/` created then emptied.
Follow-up: confirm `tests/performance/` no longer exists at HEAD. (It does not —
the tree moved to `plugins/benchmark/`.)
