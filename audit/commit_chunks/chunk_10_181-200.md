# Chunk 10 — commits 181–200

## Commits inspected
```
181 c9275be  Use dedicated throughput result for live I/O benchmark
182 828e28d  Restore complete benchmark dialog and fix I/O result rendering
183 16771dd  Refactor live benchmark sequencing into runner
184 101ffb5  Extract live benchmark workloads
185 1907e57  Extract benchmark result reporting
186 eb224c1  Simplify benchmark dialog to UI-only shell
187 68057e6  Fix benchmark refactor class extraction
188 b7938eb  Fix benchmark refactor class extraction
189 31c7920  Fix benchmark refactor class extraction
190 4f8a4e6  Keep benchmark dialog UI-only after refactor
191 2cc9f31  Fix benchmark results imports
192 984b943  Fix benchmark runner original-state restoration
193 8006656  Fix live benchmark monitor Qt parent
194 8bc63a8  Add files via upload
195 0b6b8f9  Remove live benchmark QThread monitor
196 733a802  Add files via upload
197 364a0a1  Protect live monster benchmarks with a safety skip
198 baa15d4  Skip oversized live monster workloads before loading them
199 2647540  Match live I/O throughput report to FPS result styling
200 f2c5537  Skip live monster apocalypse by default
```

## Files touched
```
      8 editor/benchmark_runner.py
      4 editor/benchmark_tests.py
      4 editor/benchmark_results.py
      4 editor/benchmark_dialog.py
```

## Analysis

Subsystems affected: benchmark only.

* **183–190: the benchmark is split into four modules** —
  `benchmark_runner.py` (sequencing), `benchmark_tests.py` (workloads),
  `benchmark_results.py` (reporting), `benchmark_dialog.py` (UI shell).
  A genuine architectural improvement.
* 187/188/189 "Fix benchmark refactor class extraction" ×3 — the split left all
  three new modules broken; each needed a repair commit. 191 fixes the imports.
* 182 "Restore complete benchmark dialog and fix I/O result rendering" —
  another truncation repair immediately before the split.
* 192 "Fix benchmark runner original-state restoration" — the editor-document
  restore (chunk 03) was broken by the refactor and repaired here.
* 193/195: a `QThread` monitor is added then removed two commits later.
* 194/196 "Add files via upload" — opaque whole-file replacements of
  `benchmark_runner.py` and `benchmark_results.py`.
* 197/198/200: safety skips for oversized monster workloads.

New mechanisms: four-module benchmark architecture.
Removed mechanisms: `QThread` monitor (195) — replaced by same-process watchdog
at 217/218, then by external IPC supervision at 221–224. **Three successive
supervision mechanisms in ~40 commits.**
Architectural concern: this four-module split is itself undone at 231
("Consolidate live benchmark implementation" → `editor/benchmark.py`) and then
partly redone at 306/307 (`benchmark_tests.py` reappears). The final
`plugins/benchmark/` tree carries *both* `benchmark.py` and
`benchmark_tests.py` — see chunk 16 and the benchmark subsystem audit.
Tests affected: none.
