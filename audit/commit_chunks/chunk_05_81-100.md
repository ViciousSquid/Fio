# Chunk 05 — commits 81–100

## Commits inspected
```
081 2f2d42b  Add benchmark preparation and measurement watchdogs
082 caf2010  Harden benchmark workload preparation limits
083 45852eb  Include Fio version in benchmark exports
084 6440464  Refine benchmark result formatting
085 3ee7276  Add daemon monitor for risky benchmarks
086 f2b2738  Isolate risky benchmarks in killable worker process
087 9a14f03  Add isolated worker mode for risky benchmarks
088 d382914  Distinguish worker timeouts from worker failures
089 eafe3e4  Use fixed seed for isolated monster benchmarks
090 426c54f  Set stress worker timeouts to 30s and apocalypse to 60s
091 0558cbd  Export benchmark results as HTML and add run throbber
092 5abb283  Polish HTML export and benchmark busy indicator lifecycle
093 3d48fa7  Include worker SysMon metrics in HTML benchmark report
094 cc222ea  Keep scalar culling fallback out of renderer benchmarks
095 93e88fb  Record VSync setting in benchmark HTML reports
096 a60ebca  Clarify VSync scope in benchmark HTML report
097 7626931  Polish VSync metadata presentation in HTML report
098 33091ef  Define benchmark Average FPS from captured frame timing
099 aeb7cc6  Document benchmark FPS calculation methodology
100 7b0ae96  Abort benchmark when no map and no tests are selected
```

## Files touched
```
     17 editor/benchmark_dialog.py
      3 tests/performance/fio_benchmark.py
```

## Analysis

Subsystems affected: benchmark only.

* 081/082 watchdogs; 085–090 **isolated killable worker processes** for "risky"
  benchmarks (monster apocalypse etc.). The benchmark is now a multi-process
  system with timeouts (30s stress / 60s apocalypse).
* 091–093 HTML export, 095–097 VSync metadata, 098/099 FPS methodology.
* 094 "Keep scalar culling fallback out of renderer benchmarks" — explicitly
  makes the benchmark exercise the **vectorised** `cull_by_distance` path only.
  Sensible: it stops the benchmark measuring the fail-open scalar fallback.
  Note this also means the **scalar path is never benchmarked** — a blind spot
  if the two paths ever diverge (they can: see renderer audit).
* 100 aborts with no map and no tests selected.

New mechanisms: worker-process isolation, HTML report, VSync provenance.
Performance concern: none in engine.
Compatibility: benchmark HTML reports embed the Fio version (083).
Follow-up: the scalar/vectorised cull divergence blind spot is a real one —
carried to the renderer subsystem audit.
