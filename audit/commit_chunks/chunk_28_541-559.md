# Chunk 28 — commits 541–559

## Commits inspected
```
541 851b22a  tidy: rewrite plugin README for core Prop architecture
542 e13d9ee  docs: remove obsolete Tidy Object terminology
543 4063930  plugins: add plugin-menu action API
544 82c7ac6  plugins: add plugin-menu action registry
545 e60f772  plugins: add plugin-menu action API
546 496cdad  plugins: render plugin menu actions
547 29bc4f0  plugins: add safe menu action dispatch
548 5685b50  tidy: add bundled demo map menu action
549 0f53942  tidy: move demo map into plugin
550 16a2dbe  tidy: remove demo map from maps folder
551 b2f37ec  tidy: update demo generator path
552 ff8e253  docs: point Tidy README at bundled demo map
553 be09087  tests: cover bundled Tidy demo menu action
554 91807fa  tidy: gate demo map action on plugin enabled state
555 4feebfc  tests: verify Tidy demo respects unsaved changes
556 d24701e  plugins: remove duplicate menu action API
557 3132a98  tidy: expose receptacle and goal property schemas
558 a490f99  tests: verify Tidy entity property schemas
559 3a78157  tidy: move demo map into plugin
```

## Files touched
```
      3 plugins/tidy/tests/test_smoke.py
      3 plugins/tidy/README.md
      3 plugins/integration.py
      3 plugins/api.py
      2 plugins/tidy/plugin.py
      1 plugins/tidy/tools/make_example_map.py
      1 plugins/tidy/Tidy_Test.json
      1 plugins/manager.py
      1 maps/Tidy_Test.json
```

## Analysis

Subsystems affected: **plugin menu API, Tidy demo map, plugin property schemas.**

* 541/542 Tidy README and terminology rewritten for the core-Prop architecture.

* **543/544/545: the plugin-menu action API is added three times.**
  "plugins: add plugin-menu action API" (543), "plugins: add plugin-menu action
  registry" (544), "plugins: add plugin-menu action API" **again** (545 — same
  subject as 543). 546 renders the actions, 547 adds safe dispatch, and
  **556 "plugins: remove duplicate menu action API"** removes the duplicate.
  A duplicated mechanism created and removed inside thirteen commits; verified
  removed at HEAD.

* 548–555: Tidy's bundled demo map.
  **549 and 559 have the identical subject "tidy: move demo map into plugin".**
  548 adds the menu action; 549 moves the map; 550 removes it from `maps/`;
  551 updates the generator path; 552 points the README at it; 553 covers the
  menu action; 554 gates the action on the plugin being enabled; 555 verifies
  the demo respects unsaved changes. 559 repeats 549's subject as the final
  commit of the range. Verified at HEAD: the map exists once, at
  `plugins/tidy/Tidy_Test.json` (git records it as `R073` from
  `maps/Tidy_Test.json`), and `maps/` no longer carries it.

* **554/555 are good practice worth noting**: the demo-map action goes through
  the editor's existing `check_unsaved_changes()` dialog rather than replacing
  the user's document. A plugin menu action that loads a map is exactly where
  unsaved work gets destroyed, and it was handled.

* 557/558 "tidy: expose receptacle and goal property schemas" + test.

New mechanisms: plugin menu actions; bundled plugin demo maps.
Removed mechanisms: the duplicate menu-action API (556).
Architectural note: plugin menu actions receive the `MainWindow`. Combined with
502's `main_window=` on console dispatch, **2.5 widens the plugin API's reach
into the editor considerably** — a plugin can now load maps, drive the console
and add menu entries. That is a deliberate direction (Benchmark needs it), but
it makes plugin trust a more meaningful question than at 2.4.2, where the
`EditorAPI` surface was registration-only. Recorded in architecture.md.
Tests affected: 553, 555, 558.
