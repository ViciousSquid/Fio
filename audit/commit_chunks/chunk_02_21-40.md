# Chunk 02 — commits 21–40

## Commits inspected
```
021 146c48f  Drive benchmark scenes from Fio procedural map generator
022 067591e  Exercise real Fio play mode and camera movement
023 a2ff37d  Use procedural Fio maps for standard renderer benchmark
024 286cd32  Add procedural monster stress test options
025 0ad6dd9  Materialize procedural benchmark maps through Fio EditorState
026 1ef26dd  Re-enable monster benchmark options after completion
027 af80644  Build benchmark I/O links through real Thing API
028 14f1caf  Fix standalone benchmark import path
029 035d42f  Add optional maximum monster apocalypse stress test
030 15476ff  Add final monster apocalypse benchmark option
031 4cbc9a6  Add programmatic SysMon benchmark metrics
032 75d1f6f  Expose runtime FPS through SysMon
033 a7e1c3d  Report benchmark performance through Fio SysMon
034 0bf578d  Fix SysMon benchmark result formatting
035 77dda71  Preserve SysMon line endings after metrics update
036 8bc3f3e  Preserve QtGameView line endings after SysMon update
037 77f7fab  Materialize benchmark I/O through production API and use SysMon as authority
038 61c6dc4  Use live Fio instance for benchmark workloads
039 9f5112f  Add SysMon benchmark session reset
040 dbd5b83  Run benchmark against live Fio instance
```

## Files touched
```
     11 tests/performance/fio_benchmark.py
      4 editor/benchmark_dialog.py
      3 engine/sysmon.py
      2 engine/qt_game_view.py
```

## Analysis

Subsystems affected: benchmark, **`engine/sysmon.py` (production)**,
**`engine/qt_game_view.py` (production)**, editor/main_window.

* 021–030: benchmark scenes driven from `editor.procedural_generator` and
  materialised through a real `EditorState` (025) and the real `Thing` I/O API
  (027). Good direction — the benchmark stops simulating the engine and starts
  using it.
* 031 **`engine/sysmon.py`: "Add programmatic SysMon benchmark metrics"** — the
  first time benchmark work enters a production runtime module. SysMon is an
  on-screen overlay on the render path.
* 032/036 **`engine/qt_game_view.py`: "Expose runtime FPS through SysMon"** —
  benchmark instrumentation lands in the GL widget.
* 035/036 are pure **CRLF line-ending restorations** — evidence the tooling
  writing these files was rewriting line endings wholesale. This recurs at
  284, 355–359, 503. Any such commit is a whole-file rewrite and can silently
  drop content; each one needs checking rather than trusting the message.
* 037–040: benchmark switches to driving a **live Fio instance** rather than a
  headless one. This is the decision that forces everything afterwards
  (watchdogs, cooperative yielding, worker processes, IPC).

New mechanisms: SysMon programmatic metrics; live-instance benchmarking.
Ownership change: **SysMon becomes the authority for benchmark FPS** (033).
Performance concern: instrumentation added to `qt_game_view` per-frame paths —
partly walked back at 280/282 ("Remove benchmark work from QtGameView hot
paths", "Make SysMon benchmark metrics on-demand"). Net at HEAD is on-demand
(`SysMon.get_metrics()`), which is correct.
Tests affected: none.
Follow-up: verify the 280/282/284 walk-back left no residue in the frame path.
**Verified at HEAD: it did not — `get_metrics()` is pull-only.**
