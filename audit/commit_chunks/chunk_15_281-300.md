# Chunk 15 — commits 281–300

## Commits inspected
```
281 a9dcf2b  Keep production I/O dispatcher free of benchmark hooks
282 004c2ad  Make SysMon benchmark metrics on-demand
283 ac1f334  Use production I/O path under external benchmark supervision
284 6a3a720  Preserve SysMon line endings in benchmark isolation diff
285 cc0ef41  Remove monster capacity benchmark
286 aad5bed  Remove monster capacity benchmark option
287 1249efc  Restore live benchmark default durations
288 407f7eb  Make benchmark completion message prominent
289 e26bc9d  Report live I/O elapsed seconds prominently
290 0a76e73  Keep large live brush preparation responsive
291 1f8ed9f  Avoid orthographic view rebuild during brush stress preparation
292 da03606  Avoid editor UI rebuild during live stress scene reset
293 547e9df  Restore full editor machinery for live brush benchmarks
294 5b71b97  Run brush benchmarks through full editor view machinery
295 925f023  Correct live stress preparation documentation
296 5c72ae1  Fix live brush benchmark measurement transition
297 2d0ee2c  Polish benchmark result separators
298 c925726  Reduce benchmark throbber height
299 73ff7f6  Highlight live I/O throughput result
300 d3f929b  Right-align and invert I/O throughput highlight
```

## Files touched
```
     12 editor/benchmark.py
      3 benchmark_manager.py
      2 tests/performance/fio_benchmark.py
      2 engine/sysmon.py
      1 editor/io_system.py
```

## Analysis

Subsystems affected: **`editor/io_system.py`**, **`engine/sysmon.py`**,
benchmark.

* **281 "Keep production I/O dispatcher free of benchmark hooks"** — reverts the
  iterative-dispatch change from chunk 08. **Verified byte-clean**: at this
  commit `editor/io_system.py` is identical to the 2.4.2 baseline.
* 282/284 finish the SysMon walk-back (on-demand metrics, line endings).
* 283 "Use production I/O path under external benchmark supervision" — the
  benchmark now drives the *real* dispatcher and relies on the external manager
  process for timeout supervision instead of hooks inside the engine. This is
  the correct final shape: supervision outside, no instrumentation inside.
* 285/286 remove the monster capacity benchmark (added at 207–209, 78 commits
  earlier).
* 290–296: brush-stress preparation is repeatedly re-tuned between "avoid editor
  rebuild" (291/292) and "restore full editor machinery" (293/294) — the
  benchmark oscillates between measuring a realistic editor and measuring a
  fast one. 294 settles on realistic.

Removed mechanisms: benchmark hooks in the production I/O dispatcher and in
SysMon — **both migrations verified complete at HEAD**.
Architectural outcome: by the end of this chunk the engine carries no
benchmark-specific code at all. That is the right end state; it took ~250
commits and three false starts to reach.
