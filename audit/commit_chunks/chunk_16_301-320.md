# Chunk 16 — commits 301–320

## Commits inspected
```
301 ce3c4b0  Remove LogicRelays from brush stress workload
302 76e2866  Fix live benchmark brush count reporting
303 1f0a20b  Fix benchmark selection and empty-project handling
304 c4c35b8  Extend monster chaos witness to 15 seconds
305 e215a70  Keep chaos monsters clear of room walls
306 3293c25  Move benchmark test implementations into benchmark_tests.py
307 af97f4c  Use benchmark_tests module for benchmark implementations
308 e9e8c26  Restore full Benchmark_dev tree with OBJ bounds diagnostics
309 a007275  Improve OBJ compatibility and imported model pivots
310 c051d85  Correct OBJ import pivot handling and path normalization
311 9b071f1  Normalize OBJ MTL texture paths
312 3e05eed  Make model cache paths robust in editor views
313 4400d76  Render models independently from sprite classification
314 dc8e3a1  Use model_path for 2D model hit testing
315 7ccbd4f  Keep OBJ GPU loading inside the 3D GL context
316 402ee64  Add files via upload
317 7f51250  Fit unusually small downloaded OBJ imports to Fio world scale
318 f0d1577  Fix OBJ CPU triangle data for editor wireframes
319 02eb17f  Use OBJ triangle indices for 2D model wireframes
320 e519047  Support OBJ base-color textures without MTL files
```

## Files touched
```
      6 engine/obj_loader.py
      4 engine/renderer_core.py
      4 editor/view_2d.py
      3 editor/benchmark.py
      2 tests/performance/fio_benchmark.py
      2 engine/renderer_F.py
      1 engine/qt_game_view.py
      1 editor/main_window.py
      1 editor/benchmark_tests.py
      1 assets/models/Barrel7.obj
      1 assets/models/Barrel 7/Barrel7_Roughness.png
      1 assets/models/Barrel 7/Barrel7_Normal.png
      1 assets/models/Barrel 7/Barrel7_Metallic.png
      1 assets/models/Barrel 7/Barrel7_Emission.png
      1 assets/models/Barrel 7/Barrel7_Displacement.png
      1 assets/models/Barrel 7/Barrel7_BaseColor.png
```

## Analysis

Subsystems affected: benchmark (closing), **`engine/obj_loader.py`**,
`editor/main_window.py`, editor views. **Phase A ends, Phase B begins.**

* 301–305 final benchmark tuning. 306/307 **re-create `benchmark_tests.py`**,
  which was deleted at 237 as part of the 231 consolidation — so the
  consolidation was never completed. At HEAD `plugins/benchmark/` carries both
  `benchmark.py` and `benchmark_tests.py`.
* **308 "Restore full Benchmark_dev tree with OBJ bounds diagnostics"** touches
  `engine/obj_loader.py` and opens the OBJ-import phase.
* 309–321: real improvements to OBJ handling — pivot handling and path
  normalisation (310), MTL texture paths (311, 324, 326), base-colour textures
  without MTL (320), fallback textures for `usemtl` groups (321), CPU triangle
  data for editor wireframes (318/319).
* **313 "Render models independently from sprite classification"** and
  314 "Use model_path for 2D model hit testing" — a genuine ownership change:
  model rendering stops being decided by sprite classification. This is the
  groundwork for 342–344 (removing per-frame classification entirely).
* **315 "Keep OBJ GPU loading inside the 3D GL context"** — a real GL-context
  correctness fix; uploading buffers outside the context is undefined.
* **317 "Fit unusually small downloaded OBJ imports to Fio world scale"**
  (`editor/main_window.py`) — survives to HEAD. It re-parses the whole OBJ a
  second time at import purely to measure its extent. One-off cost on an
  explicit user action, so acceptable, but it is a duplicate parse.

Net effect on `engine/obj_loader.py`: +460 / −286, the largest single-file
change outside the renderer.
Compatibility: import-time auto-scale applies only at import, so **authored
maps with explicit scales are untouched** — verified by reading the guard
(`all(float(v) == 1.0 for v in initial_scale)`).
