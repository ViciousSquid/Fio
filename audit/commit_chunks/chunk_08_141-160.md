# Chunk 08 — commits 141–160

## Commits inspected
```
141 95bf9b4  Fix benchmark dialog diagnostics string literal
142 f13fadd  Fix worker JSON serialization of NumPy benchmark metrics
143 52b208e  Only run current-world phases when a map is loaded
144 728b6bb  Do not launch a second Fio instance for monster apocalypse
145 bdc9d2b  Change greeting from 'Hello World' to 'Goodbye World'
146 8a25b52  Run stress benchmarks live in the existing Fio window
147 e239141  Align combined FPS result with live benchmark result boxes
148 7b835dd  Add hard timeout watchdog for live stress benchmarks
149 106571f  Keep live stress watchdog armed through cleanup
150 b2eca64  Make live stress benchmarks non-destructive and Qt-thread driven
151 0dcb4a9  Start live stress timeout supervision before workload preparation
152 b9fc991  Restore benchmark startup state and queue live stress tests
153 7384304  Fix benchmark dialog syntax and restore reporting tail
154 e90a437  Restore corrupted performance benchmark module
155 13c4b8a  Fix malformed deadline loop in benchmark helper
156 c93608d  Benchmark: add cooperative iterative I/O dispatch
157 b1ae879  Benchmark: return live brush workload as level data
158 521c3c8  Benchmark: supervise live I/O cooperatively
159 5648e29  Benchmark: return cooperative I/O dispatch status
160 b2540d7  Raise live stress benchmark timeout to 60 seconds
```

## Files touched
```
     13 editor/benchmark_dialog.py
      5 tests/performance/fio_benchmark.py
      2 editor/io_system.py
```

## Analysis

Subsystems affected: benchmark, **`editor/io_system.py` (production I/O core)**.

**This chunk contains the single most invasive benchmark change.**

* 145 "Change greeting from 'Hello World' to 'Goodbye World'" — a benchmark
  dialog string. Harmless, but an example of unreviewed noise in the history.
* 146–152: stress benchmarks move back **into the live Fio window** (undoing
  the worker-process isolation of 085–090 for the stress set). Competing
  mechanisms: external worker processes and in-window live runs now both exist.
* 153/154 truncation-and-restore pair again ("Fix benchmark dialog syntax and
  restore reporting tail", "Restore corrupted performance benchmark module").
* **156 `editor/io_system.py`: "Benchmark: add cooperative iterative I/O
  dispatch"** — `IOManager.fire_output` is rewritten. It gains
  `_iterative=True`, a `_dispatch_queue` deque, `set_dispatch_yield_hook`,
  and the real body is renamed `_fire_output_one` with an
  `activator_id_override`. **The production I/O dispatcher is modified to serve
  a benchmark.** Worse, as committed at 156 the `_iterative` branch falls off
  the end of the function without returning `completed` — it returns `None`.
* 159 "Benchmark: return cooperative I/O dispatch status" fixes that
  (`return completed`).
* **Resolution: 281 reverts all of it.** VERIFIED: `git diff
  origin/2.4.2.1709_Latest a9dcf2b -- editor/io_system.py` is **empty** — the
  revert restored `editor/io_system.py` byte-for-byte to the 2.4.2 baseline.
  No residue, no behaviour change, activator semantics intact.

Important changes: production I/O dispatcher temporarily grew a second
dispatch mode (queue-based, non-recursive) alongside the recursive one.
New mechanisms: iterative I/O dispatch (temporary, 156–281).
Removed mechanisms: same, at 281 — **migration complete and verified clean**.
Confirmed bugs (historical, fixed in-phase): 156's missing return value.
Architectural concern: for 125 commits, `fire_output` had **two competing
dispatch strategies** with different activator-propagation code paths. Any
behavioural difference between them was live in the editor during that window.
Tests affected: none were added for the iterative path — it was never covered.
