# Chunk 04 — commits 61–80

## Commits inspected
```
061 9fc8a5d  Fix benchmark result formatting and deterministic monster seed
062 b3c99f5  Clarify benchmark scope and collapse stress tests
063 2f0607d  Use Qt enums for benchmark stress toggle
064 6be0b30  Capture triangle statistics during benchmarks
065 af8609e  Add Select all stress tests control
066 c7d0c09  Calculate average triangle and culling metrics
067 e722c2a  Show baseline triangle and culling statistics
068 27afcb8  Refine benchmark stress-test disclosure control
069 bfc87f7  Capture live triangle stats for benchmark
070 626bb6d  Move benchmark camera through current map
071 6d788a3  Make current-world benchmark duration map-scale dependent
072 dff1d43  Cap current-world benchmark duration at 20 seconds
073 747abf9  Add real borderless and fullscreen live benchmark tests
074 5b20026  Fix benchmark window mode state indentation
075 b8fb65c  Include window mode tests in benchmark controls
076 7c6c00f  Add windowed editor 3D pane benchmark tests
077 86a8f72  Show editor viewport during current-world benchmark
078 f459439  Make benchmark average FPS result prominent
079 2e5a5bc  Drive current-world benchmark camera through LogicThread
080 f000d5e  Keep benchmark dialog visible and constrain test list
```

## Files touched
```
     18 editor/benchmark_dialog.py
      1 engine/sysmon.py
      1 engine/qt_game_view.py
```

## Analysis

Subsystems affected: benchmark, sysmon, qt_game_view.

* 064 `engine/sysmon.py` "Capture triangle statistics during benchmarks" and
  069 `engine/qt_game_view.py` "Capture live triangle stats for benchmark" —
  more instrumentation in production render paths. Both later removed/made
  on-demand (280, 282).
* 070–072, 077, 079: the "current world" benchmark that sweeps a camera through
  the user's loaded map. 079 drives the camera **through `LogicThread`** rather
  than poking the renderer — the right call for parity, but it means the
  benchmark now mutates logic-thread camera state from the Qt thread.
* 073/075/076: window-mode (borderless/fullscreen) benchmarks — these resize
  the real window mid-run; renderer resize/reload behaviour is exercised here
  incidentally, which is useful coverage but is not asserted anywhere.

Performance concern: triangle-stat capture on the render path (064/069).
Resolved later.
Threading concern: **benchmark camera control writes to `LogicThread` state
from the Qt thread** (079). No lock is taken. Carried into
`plugins/benchmark/` at HEAD — see performance.md / threading findings.
Tests affected: none.
